import requests
import time

while True:
    try:
        response = requests.get("http://192.168.100.1:5000")
        print(response.status_code)
    except requests.exceptions.RequestException:
        print("Error")

    time.sleep(1)
